// Constantes del feature de areas de competencia (catalogo solo-lectura).
// No tiene mensajes de error propios: el catalogo siempre devuelve la lista
// completa (vacia si no hay registros).
export {}
